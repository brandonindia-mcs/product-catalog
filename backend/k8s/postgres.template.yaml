apiVersion: v1
kind: Service
metadata:
  namespace: $NAMESPACE
  name: pg-service
spec:
  ports:
  - port: 5432
  selector:
    app: postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: $NAMESPACE
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: $HUB/$REPOSITORY:$TAG
        env:
        - name: POSTGRES_DB
          value: catalog
        - name: POSTGRES_USER
          value: catalog
        - name: POSTGRES_PASSWORD
          value: catalog
        ports:
        - containerPort: 5432
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
